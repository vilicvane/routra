import type {FunctionComponent} from 'react';
import React from 'react';
import type {ViewEntry} from 'routra';

export interface RouteViewProps {
  entry: ViewEntry;
}

export const RouteView: FunctionComponent<RouteViewProps> = ({}) => {
  return <></>;
};
